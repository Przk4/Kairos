server {
    listen 80;
    server_name TU_DOMINIO_O_IP;  # Cambiar por tu IP del Droplet o dominio

    # Redirigir HTTP a HTTPS (descomentar cuando tengas SSL)
    # return 301 https://$host$request_uri;

    location /static/ {
        alias /home/ubuntu/Kairos/staticfiles/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts largos para DeepSeek API
        proxy_connect_timeout 120s;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }
}

# HTTPS (descomentar después de configurar certbot)
# server {
#     listen 443 ssl;
#     server_name TU_DOMINIO;
#
#     ssl_certificate /etc/letsencrypt/live/TU_DOMINIO/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/TU_DOMINIO/privkey.pem;
#
#     location /static/ {
#         alias /home/ubuntu/Kairos/staticfiles/;
#         expires 30d;
#     }
#
#     location / {
#         proxy_pass http://127.0.0.1:8000;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 120s;
#         proxy_read_timeout 120s;
#     }
# }
